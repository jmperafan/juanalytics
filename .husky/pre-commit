#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running pre-commit checks..."

# Check TypeScript
echo "Checking TypeScript..."
npx astro check --exit-code 1

if [ $? -ne 0 ]; then
  echo "TypeScript check failed!"
  exit 1
fi

echo "Pre-commit checks passed!"
